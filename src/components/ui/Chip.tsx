export const Chip = ({ title }: { title: string }) => {
    return (
        <div className="px-8 py-5 bg-snow-white rounded-full text-xl font-light max-w-max">
            {title}
        </div>
    )
}